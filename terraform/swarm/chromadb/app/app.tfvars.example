# Copy to /mnt/eapp/code/homelab/.config/terraform/swarm/chromadb/app.tfvars (or pass via --tfvars).
#
# Docker SSH + registry credentials: /mnt/eapp/code/homelab/.config/terraform/providers/docker_arm64.tfvars
# (`swarm_docker_provider_config`). Override via `provider_config` in this file if needed.
#
# Image, Swarm publish port, and data volume name are pinned in main.tf (chromadb/chroma:1.5.9).

endpoint_host = "192.168.1.120"
