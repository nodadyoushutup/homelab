# YAML (homelab overlay)

YAML habits (key order, labels, runtime sanity checks) are in the framework
**Generic Code Agent** system prompt (**Language and format conventions — YAML**).

## This repository

- Manifests appear in Kubernetes layouts, Compose files, GitHub Actions, Terraform
  where applicable, and similar.
- For **Argo CD**-managed paths, remember GitOps flow: manifest changes are the
  source of truth; note if a live cluster action is still required.
