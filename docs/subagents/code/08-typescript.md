# TypeScript / React (homelab overlay)

TypeScript and React conventions are in the framework **Generic Code Agent**
system prompt (**Language and format conventions — TypeScript / React**).

## This repository

- The main front-end app is **`applications/langchain-agent-chat/`**. Follow hooks,
  data fetching, component structure, and styling patterns already used there.
