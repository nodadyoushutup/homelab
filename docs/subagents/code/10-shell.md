# Shell (homelab overlay)

POSIX shell practices are in the framework **Generic Code Agent** system prompt
(**Language and format conventions — Shell**).

## This repository

- Prefer repo-root-relative paths or env vars documented next to sibling scripts.
- When scripts in the same directory are idempotent or share install patterns,
  match them.
