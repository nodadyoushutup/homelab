# Dockerfile (homelab overlay)

Container build conventions are in the framework **Generic Code Agent** system
prompt (**Language and format conventions — Dockerfile**).

## This repository

- Pin base images and non-root users consistently with **sibling Dockerfiles** for
  the same service under **`applications/`**.
- Document non-obvious build args or secrets in comments or the service README.
