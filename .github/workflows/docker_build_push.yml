name: Docker - Build and Push Image

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Version to publish"
        required: true
      target_registry:
        description: "Registry target for this publish"
        required: true
        default: harbor
        type: choice
        options:
          - github
          - harbor
      build_target:
        description: "Build target"
        required: true
        type: choice
        options:
          - langchain-agent-chat
          - langgraph
          - harbor-runtime-set
          - mcp-bash-pipeline
          - mcp-cloudflare
          - mcp-ast-grep
          - mcp-filesystem
          - mcp-fortigate
          - mcp-git
          - mcp-github
          - mcp-google-workspace
          - mcp-terraform
          - gha-runner
          - jenkins-agent
          - jenkins-controller

permissions:
  contents: write
  packages: write

env:
  GHCR_NAMESPACE: ghcr.io/${{ github.repository_owner }}
  HARBOR_REGISTRY: harbor.nodadyoushutup.com

jobs:
  build:
    runs-on: [self-hosted, linux, homelab]
    outputs:
      version: ${{ steps.prepare.outputs.version }}
      digest: ${{ steps.image_metadata.outputs.digest }}
      target_registry: ${{ steps.prepare.outputs.target_registry }}
      build_strategy: ${{ steps.prepare.outputs.build_strategy }}
      image_base: ${{ steps.prepare.outputs.image_base }}
      image_name: ${{ steps.prepare.outputs.image_name }}
      docker_context: ${{ steps.prepare.outputs.docker_context }}
      dockerfile: ${{ steps.prepare.outputs.dockerfile }}
      platforms: ${{ steps.prepare.outputs.platforms }}
      registry_namespace: ${{ steps.prepare.outputs.registry_namespace }}
      publish_path_mode: ${{ steps.prepare.outputs.publish_path_mode }}

    steps:
      - name: Check out the code
        uses: actions/checkout@v6

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        with:
          install: true

      - name: Prepare build parameters
        id: prepare
        run: |
          version="${{ github.event.inputs.version }}"
          target_registry="${{ github.event.inputs.target_registry }}"
          build_target="${{ github.event.inputs.build_target }}"
          platforms="linux/amd64,linux/arm64"

          if [ -z "$version" ]; then
            echo "Version could not be determined" >&2
            exit 1
          fi

          case "$build_target" in
            langchain-agent-chat)
              image_name="langchain-agent-chat"
              docker_context="applications/langchain-agent-chat"
              platforms="linux/amd64"
              build_strategy="direct"
              ;;
            langgraph)
              image_name="langgraph"
              docker_context="applications/langgraph"
              dockerfile="applications/langgraph/Dockerfile"
              platforms="linux/amd64"
              build_strategy="direct"
              ;;
            harbor-runtime-set)
              image_name="harbor-runtime-set"
              docker_context="applications/harbor"
              build_strategy="harbor-runtime-set"
              ;;
            mcp-bash-pipeline)
              image_name="mcp-bash-pipeline"
              docker_context="applications/mcp-bash-pipeline"
              build_strategy="direct"
              ;;
            mcp-cloudflare)
              image_name="mcp-cloudflare"
              docker_context="applications/mcp-cloudflare"
              build_strategy="direct"
              ;;
            mcp-ast-grep)
              image_name="mcp-ast-grep"
              docker_context="applications/mcp-ast-grep"
              build_strategy="direct"
              ;;
            mcp-filesystem)
              image_name="mcp-filesystem"
              docker_context="applications/mcp-filesystem"
              build_strategy="direct"
              ;;
            mcp-fortigate)
              image_name="mcp-fortigate"
              docker_context="applications/mcp-fortigate"
              build_strategy="direct"
              ;;
            mcp-git)
              image_name="mcp-git"
              docker_context="applications/mcp-git"
              build_strategy="direct"
              ;;
            mcp-github)
              image_name="mcp-github"
              docker_context="applications/mcp-github"
              build_strategy="direct"
              ;;
            mcp-google-workspace)
              image_name="mcp-google-workspace"
              docker_context="applications/mcp-google-workspace"
              build_strategy="direct"
              ;;
            mcp-terraform)
              image_name="mcp-terraform"
              docker_context="applications/mcp-terraform"
              build_strategy="direct"
              ;;
            gha-runner)
              image_name="gha-runner"
              docker_context="."
              dockerfile="applications/gha-runner/Dockerfile"
              build_strategy="direct"
              ;;
            jenkins-agent)
              image_name="jenkins-agent"
              docker_context="."
              dockerfile="applications/jenkins/agent/Dockerfile"
              build_strategy="direct"
              ;;
            jenkins-controller)
              image_name="jenkins-controller"
              docker_context="."
              dockerfile="applications/jenkins/controller/Dockerfile"
              build_strategy="direct"
              ;;
            *)
              echo "Unsupported build target: $build_target" >&2
              exit 1
              ;;
          esac

          if [ "${build_strategy}" = "direct" ]; then
            dockerfile="${dockerfile:-${docker_context}/Dockerfile}"

            if [ ! -f "$dockerfile" ]; then
              echo "Dockerfile not found: $dockerfile" >&2
              exit 1
            fi
          fi

          case "$target_registry" in
            github)
              registry_namespace="${GHCR_NAMESPACE}"
              publish_path_mode="namespace-component"
              if [ "${build_strategy}" = "direct" ]; then
                image_base="${GHCR_NAMESPACE}/${image_name}"
              fi
              ;;
            harbor)
              registry_namespace="${HARBOR_REGISTRY}"
              publish_path_mode="project-per-image"
              if [ "${build_strategy}" = "direct" ]; then
                image_base="${HARBOR_REGISTRY}/${image_name}/${image_name}"
              fi
              ;;
            *)
              echo "Unsupported target_registry: $target_registry" >&2
              exit 1
              ;;
          esac

          dockerfile="${dockerfile:-}"
          image_base="${image_base:-}"

          echo "version=$version" >> "$GITHUB_OUTPUT"
          echo "target_registry=$target_registry" >> "$GITHUB_OUTPUT"
          echo "build_strategy=$build_strategy" >> "$GITHUB_OUTPUT"
          echo "image_name=$image_name" >> "$GITHUB_OUTPUT"
          echo "docker_context=$docker_context" >> "$GITHUB_OUTPUT"
          echo "dockerfile=$dockerfile" >> "$GITHUB_OUTPUT"
          echo "platforms=$platforms" >> "$GITHUB_OUTPUT"
          echo "image_base=$image_base" >> "$GITHUB_OUTPUT"
          echo "registry_namespace=$registry_namespace" >> "$GITHUB_OUTPUT"
          echo "publish_path_mode=$publish_path_mode" >> "$GITHUB_OUTPUT"

          if [ "${build_strategy}" = "direct" ]; then
            echo "::notice::Preparing ${image_name}:${version} from ${docker_context} using ${dockerfile} for ${image_base}"
          else
            echo "::notice::Preparing Harbor runtime set:${version} from ${docker_context} for ${registry_namespace} using ${publish_path_mode}"
          fi

      - name: Log in to GitHub Container Registry
        if: steps.prepare.outputs.target_registry == 'github'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Validate Harbor credentials
        if: steps.prepare.outputs.target_registry == 'harbor'
        env:
          HARBOR_USERNAME: ${{ secrets.HARBOR_ROBOT_USERNAME }}
          HARBOR_PASSWORD: ${{ secrets.HARBOR_ROBOT_SECRET }}
        run: |
          if [ -z "$HARBOR_USERNAME" ] || [ -z "$HARBOR_PASSWORD" ]; then
            echo "HARBOR_ROBOT_USERNAME and HARBOR_ROBOT_SECRET are required for Harbor publishes." >&2
            exit 1
          fi

      - name: Log in to Harbor Registry
        if: steps.prepare.outputs.target_registry == 'harbor'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.HARBOR_REGISTRY }}
          username: ${{ secrets.HARBOR_ROBOT_USERNAME }}
          password: ${{ secrets.HARBOR_ROBOT_SECRET }}

      - name: Build multi-platform image and push by digest
        if: steps.prepare.outputs.build_strategy == 'direct'
        env:
          BUILDKIT_NO_CLIENT_TOKEN: "true"
        run: |
          docker buildx build \
            --platform "${{ steps.prepare.outputs.platforms }}" \
            --file "${{ steps.prepare.outputs.dockerfile }}" \
            --metadata-file image-metadata.json \
            --output=type=image,name=${{ steps.prepare.outputs.image_base }},push=true,push-by-digest=true \
            "${{ steps.prepare.outputs.docker_context }}"

      - name: Capture image digest
        id: image_metadata
        if: steps.prepare.outputs.build_strategy == 'direct'
        run: |
          digest="$(jq -r '.["containerimage.digest"]' image-metadata.json)"

          if [ -z "$digest" ] || [ "$digest" = "null" ]; then
            echo "Image digest could not be determined" >&2
            cat image-metadata.json >&2 || true
            exit 1
          fi

          echo "digest=$digest" >> "$GITHUB_OUTPUT"

      - name: Build and publish Harbor runtime set
        if: steps.prepare.outputs.build_strategy == 'harbor-runtime-set'
        env:
          DOCKER_CLI_EXPERIMENTAL: enabled
        run: |
          chmod 0755 applications/harbor/scripts/build-multiarch.sh
          applications/harbor/scripts/build-multiarch.sh \
            --namespace "${{ steps.prepare.outputs.registry_namespace }}" \
            --tag "${{ steps.prepare.outputs.version }}" \
            --path-mode "${{ steps.prepare.outputs.publish_path_mode }}" \
            --push

  push:
    runs-on: [self-hosted, linux, homelab]
    needs: build
    if: needs.build.outputs.build_strategy == 'direct'

    steps:
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        with:
          install: true

      - name: Log in to GitHub Container Registry
        if: needs.build.outputs.target_registry == 'github'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Log in to Harbor Registry
        if: needs.build.outputs.target_registry == 'harbor'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.HARBOR_REGISTRY }}
          username: ${{ secrets.HARBOR_ROBOT_USERNAME }}
          password: ${{ secrets.HARBOR_ROBOT_SECRET }}

      - name: Publish multi-platform manifest
        env:
          BUILDKIT_NO_CLIENT_TOKEN: "true"
        run: |
          digest="${{ needs.build.outputs.digest }}"
          version="${{ needs.build.outputs.version }}"
          image_base="${{ needs.build.outputs.image_base }}"

          if [ -z "$digest" ] || [ -z "$version" ] || [ -z "$image_base" ]; then
            echo "Digest, version, or image base is missing" >&2
            exit 1
          fi

          docker buildx imagetools create \
            --tag ${image_base}:${version} \
            --tag ${image_base}:latest \
            ${image_base}:${version}@${digest}
