name: homelab-dev

services:
  langgraph-dev:
    image: homelab/langgraph:latest
    pull_policy: never
    build:
      context: ../applications/langgraph
      dockerfile: Dockerfile
    environment:
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      CODE_REPOSITORY_ROOT: /mnt/eapp/code/homelab
      PYTHONDONTWRITEBYTECODE: "1"
      PYTHONUNBUFFERED: "1"
      PIP_NO_CACHE_DIR: "1"
    ports:
      - "2124:2024"
    volumes:
      - ../applications/langgraph:/app/langgraph
      - langgraph_state:/app/langgraph/src/agents/langgraph/.langgraph_api

  langchain-agent-chat-dev:
    image: homelab/langchain-agent-chat:latest
    pull_policy: never
    build:
      context: ../applications/langchain-agent-chat
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_API_URL: http://192.168.1.36:3000/api
        NEXT_PUBLIC_ASSISTANT_ID: langgraph
        NEXT_PUBLIC_AUTH_SCHEME: ""
        LANGGRAPH_API_URL: http://langgraph-dev:2024
    depends_on:
      - langgraph-dev
    environment:
      NEXT_PUBLIC_API_URL: http://192.168.1.36:3000/api
      NEXT_PUBLIC_ASSISTANT_ID: langgraph
      NEXT_PUBLIC_AUTH_SCHEME: ""
      LANGGRAPH_API_URL: http://langgraph-dev:2024
      NEXT_TELEMETRY_DISABLED: "1"
    ports:
      - "3000:3000"
volumes:
  langgraph_state:
