apiVersion: v1
kind: Service
metadata:
  name: mcp-ast-grep
  namespace: mcp-ast-grep
  annotations:
    argocd.argoproj.io/sync-wave: "35"
spec:
  selector:
    app.kubernetes.io/name: mcp-ast-grep
  ports:
    - name: http
      port: 8096
      targetPort: http
