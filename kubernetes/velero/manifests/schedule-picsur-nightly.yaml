# TEMP: initial Kopia seed run — 5 min stagger from deploy time.
# Restore production crons: scripts/misc/velero_schedules_restore_production.sh

apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: picsur-nightly
  namespace: velero
  annotations:
    argocd.argoproj.io/sync-wave: "20"
spec:
  schedule: "43 23 * * *"
  template:
    snapshotMoveData: true
    includedNamespaces:
      - picsur
    storageLocation: picsur
    ttl: 336h0m0s
