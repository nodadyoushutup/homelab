apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mcp-cloudflare
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "46"
spec:
  project: mcp-cloudflare
  destination:
    server: https://kubernetes.default.svc
    namespace: mcp-cloudflare
  source:
    repoURL: git@github.com:nodadyoushutup/homelab.git
    targetRevision: HEAD
    path: kubernetes/mcp-cloudflare
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
