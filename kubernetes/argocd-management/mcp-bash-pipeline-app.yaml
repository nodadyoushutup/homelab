apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mcp-bash-pipeline
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "46"
spec:
  project: mcp-bash-pipeline
  destination:
    server: https://kubernetes.default.svc
    namespace: mcp-bash-pipeline
  source:
    repoURL: git@github.com:nodadyoushutup/homelab.git
    targetRevision: HEAD
    path: kubernetes/mcp-bash-pipeline
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
