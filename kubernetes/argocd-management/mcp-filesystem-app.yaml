apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mcp-filesystem
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "46"
spec:
  project: mcp-filesystem
  destination:
    server: https://kubernetes.default.svc
    namespace: mcp-filesystem
  source:
    repoURL: git@github.com:nodadyoushutup/homelab.git
    targetRevision: HEAD
    path: kubernetes/mcp-filesystem
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
