apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mcp-atlassian
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "46"
spec:
  project: mcp-atlassian
  destination:
    server: https://kubernetes.default.svc
    namespace: mcp-atlassian
  source:
    repoURL: git@github.com:nodadyoushutup/homelab.git
    targetRevision: HEAD
    path: kubernetes/mcp-atlassian
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
