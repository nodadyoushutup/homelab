apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: mcp-argocd-vault
  namespace: mcp-argocd
  annotations:
    argocd.argoproj.io/sync-wave: "10"
spec:
  provider:
    vault:
      server: http://192.168.1.120:8200
      path: secret
      version: v2
      auth:
        tokenSecretRef:
          name: mcp-argocd-vault-reader
          key: VAULT_TOKEN
