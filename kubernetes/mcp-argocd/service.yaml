apiVersion: v1
kind: Service
metadata:
  name: mcp-argocd
  namespace: mcp-argocd
  annotations:
    argocd.argoproj.io/sync-wave: "35"
spec:
  selector:
    app.kubernetes.io/name: mcp-argocd
  ports:
    - name: http
      port: 3000
      targetPort: http
