apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: mcp-bash-pipeline-registry
  namespace: mcp-bash-pipeline
  annotations:
    argocd.argoproj.io/sync-wave: "15"
spec:
  refreshInterval: 1m
  secretStoreRef:
    kind: SecretStore
    name: mcp-bash-pipeline-vault
  target:
    name: mcp-bash-pipeline-registry
    creationPolicy: Owner
    deletionPolicy: Retain
    template:
      engineVersion: v2
      mergePolicy: Replace
      type: kubernetes.io/dockerconfigjson
      data:
        .dockerconfigjson: |
          {"auths":{"{{ .registry }}":{"username":"{{ .username }}","password":"{{ .password }}","auth":"{{ printf "%s:%s" .username .password | b64enc }}"}}}
  data:
    - secretKey: registry
      remoteRef:
        key: k8s/mcp_bash_pipeline
        property: harbor_registry
        conversionStrategy: Default
        decodingStrategy: None
        metadataPolicy: None
    - secretKey: username
      remoteRef:
        key: k8s/mcp_bash_pipeline
        property: harbor_username
        conversionStrategy: Default
        decodingStrategy: None
        metadataPolicy: None
    - secretKey: password
      remoteRef:
        key: k8s/mcp_bash_pipeline
        property: harbor_password
        conversionStrategy: Default
        decodingStrategy: None
        metadataPolicy: None
