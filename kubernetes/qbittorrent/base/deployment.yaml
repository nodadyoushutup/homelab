apiVersion: apps/v1
kind: Deployment
metadata:
  name: qbittorrent
  annotations:
    argocd.argoproj.io/sync-wave: "30"
spec:
  replicas: 1
  revisionHistoryLimit: 0
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: qbittorrent
  template:
    metadata:
      labels:
        app.kubernetes.io/name: qbittorrent
    spec:
      securityContext:
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      nodeSelector:
        kubernetes.io/hostname: k8s-wk-0
      initContainers:
        - name: render-qbittorrent-config
          image: busybox:1.36
          imagePullPolicy: IfNotPresent
          command:
            - /bin/sh
            - -lc
            - |
              set -eu
              mkdir -p /config/qBittorrent
              cp /config-template/qBittorrent.conf.tmpl /tmp/qBittorrent.conf

              escape_sed_replacement() {
                printf '%s' "$1" | sed -e 's/[\\/&]/\\&/g'
              }

              sed -i \
                -e "s/__WEBUI_PASSWORD_PBKDF2__/$(escape_sed_replacement "$WEBUI_PASSWORD_PBKDF2")/g" \
                -e "s/__WEBUI_PORT__/$(escape_sed_replacement "$WEBUI_PORT")/g" \
                -e "s/__TORRENTING_PORT__/$(escape_sed_replacement "$TORRENTING_PORT")/g" \
                /tmp/qBittorrent.conf

              cp /tmp/qBittorrent.conf /config/qBittorrent/qBittorrent.conf
              chmod 600 /config/qBittorrent/qBittorrent.conf
          env:
            - name: WEBUI_PORT
              valueFrom:
                configMapKeyRef:
                  name: qbittorrent-runtime
                  key: WEBUI_PORT
            - name: TORRENTING_PORT
              valueFrom:
                configMapKeyRef:
                  name: qbittorrent-runtime
                  key: TORRENTING_PORT
            - name: WEBUI_PASSWORD_PBKDF2
              valueFrom:
                secretKeyRef:
                  name: qbittorrent-secrets
                  key: WEBUI_PASSWORD_PBKDF2
          volumeMounts:
            - name: qbittorrent-config
              mountPath: /config
            - name: qbittorrent-config-template
              mountPath: /config-template
              readOnly: true
      containers:
        - name: qbittorrent
          image: lscr.io/linuxserver/qbittorrent:4.6.7
          imagePullPolicy: IfNotPresent
          env:
            - name: TZ
              value: America/New_York
            - name: PUID
              value: "1000"
            - name: PGID
              value: "1000"
            - name: WEBUI_PORT
              valueFrom:
                configMapKeyRef:
                  name: qbittorrent-runtime
                  key: WEBUI_PORT
            - name: TORRENTING_PORT
              valueFrom:
                configMapKeyRef:
                  name: qbittorrent-runtime
                  key: TORRENTING_PORT
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
            - name: bittorrent-tcp
              containerPort: 10096
              protocol: TCP
            - name: bittorrent-udp
              containerPort: 10096
              protocol: UDP
          volumeMounts:
            - name: qbittorrent-config
              mountPath: /config
            - name: qbittorrent-downloads
              mountPath: /downloads
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 45
            periodSeconds: 20
      volumes:
        - name: qbittorrent-config
          persistentVolumeClaim:
            claimName: qbittorrent-config
        - name: qbittorrent-downloads
          persistentVolumeClaim:
            claimName: qbittorrent-downloads
        - name: qbittorrent-config-template
          configMap:
            name: qbittorrent-config-template
