apiVersion: v1
kind: Service
metadata:
  name: mcp-git
  namespace: mcp-git
  annotations:
    argocd.argoproj.io/sync-wave: "35"
spec:
  selector:
    app.kubernetes.io/name: mcp-git
  ports:
    - name: http
      port: 8099
      targetPort: http
