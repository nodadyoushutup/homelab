apiVersion: apps/v1
kind: Deployment
metadata:
  name: radarr
  namespace: radarr
  annotations:
    argocd.argoproj.io/sync-wave: "30"
spec:
  replicas: 1
  revisionHistoryLimit: 0
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: radarr
  template:
    metadata:
      labels:
        app.kubernetes.io/name: radarr
    spec:
      securityContext:
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      nodeSelector:
        kubernetes.io/hostname: k8s-wk-0
      initContainers:
        - name: wait-for-postgres
          image: busybox:1.38
          imagePullPolicy: IfNotPresent
          securityContext:
            runAsUser: 65534
            runAsGroup: 65534
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          command:
            - /bin/sh
            - -lc
            - |
              set -eu
              until nc -z radarr-postgres 5432; do
                echo "Waiting for radarr-postgres..."
                sleep 2
              done
      containers:
        - name: radarr
          image: lscr.io/linuxserver/radarr:6.3.0
          imagePullPolicy: IfNotPresent
          env:
            - name: TZ
              value: America/New_York
            - name: PUID
              value: "1000"
            - name: PGID
              value: "1000"
            - name: RADARR__POSTGRES__HOST
              value: radarr-postgres
            - name: RADARR__POSTGRES__PORT
              value: "5432"
            - name: RADARR__POSTGRES__USER
              valueFrom:
                secretKeyRef:
                  name: radarr-secrets
                  key: POSTGRES_USER
            - name: RADARR__POSTGRES__PASSWORD
              valueFrom:
                secretKeyRef:
                  name: radarr-secrets
                  key: POSTGRES_PASSWORD
            - name: RADARR__POSTGRES__MAINDB
              valueFrom:
                secretKeyRef:
                  name: radarr-secrets
                  key: POSTGRES_DB
            - name: RADARR__POSTGRES__LOGDB
              valueFrom:
                secretKeyRef:
                  name: radarr-secrets
                  key: POSTGRES_LOG_DB
          ports:
            - name: http
              containerPort: 7878
              protocol: TCP
          volumeMounts:
            - name: radarr-config
              mountPath: /config
            - name: media
              mountPath: /media
          readinessProbe:
            httpGet:
              path: /ping
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /ping
              port: http
            initialDelaySeconds: 30
            periodSeconds: 20
      volumes:
        - name: radarr-config
          persistentVolumeClaim:
            claimName: radarr-config-v2
        - name: media
          nfs:
            server: 192.168.1.100
            path: /mnt/epool/media
