apiVersion: v1
kind: Service
metadata:
  name: radarr-postgres
  namespace: radarr
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: radarr-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
      protocol: TCP
