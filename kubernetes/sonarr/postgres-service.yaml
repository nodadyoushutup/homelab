apiVersion: v1
kind: Service
metadata:
  name: sonarr-postgres
  namespace: sonarr
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: sonarr-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
      protocol: TCP
