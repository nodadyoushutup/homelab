apiVersion: apps/v1
kind: Deployment
metadata:
  name: radarr-4k
  namespace: radarr-4k
  annotations:
    argocd.argoproj.io/sync-wave: "30"
spec:
  replicas: 1
  revisionHistoryLimit: 0
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: radarr-4k
  template:
    metadata:
      labels:
        app.kubernetes.io/name: radarr-4k
    spec:
      securityContext:
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      nodeSelector:
        kubernetes.io/hostname: k8s-wk-0
      initContainers:
        - name: wait-for-postgres
          image: busybox:1.36
          imagePullPolicy: IfNotPresent
          securityContext:
            runAsUser: 65534
            runAsGroup: 65534
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          command:
            - /bin/sh
            - -lc
            - |
              set -eu
              until nc -z radarr-4k-postgres 5432; do
                echo "Waiting for radarr-4k-postgres..."
                sleep 2
              done
      containers:
        - name: radarr-4k
          image: lscr.io/linuxserver/radarr:latest
          imagePullPolicy: IfNotPresent
          env:
            - name: TZ
              value: America/New_York
            - name: PUID
              value: "1000"
            - name: PGID
              value: "1000"
            - name: RADARR__POSTGRES__HOST
              value: radarr-4k-postgres
            - name: RADARR__POSTGRES__PORT
              value: "5432"
            - name: RADARR__POSTGRES__USER
              valueFrom:
                secretKeyRef:
                  name: radarr-4k-secrets
                  key: POSTGRES_USER
            - name: RADARR__POSTGRES__PASSWORD
              valueFrom:
                secretKeyRef:
                  name: radarr-4k-secrets
                  key: POSTGRES_PASSWORD
            - name: RADARR__POSTGRES__MAINDB
              valueFrom:
                secretKeyRef:
                  name: radarr-4k-secrets
                  key: POSTGRES_DB
            - name: RADARR__POSTGRES__LOGDB
              valueFrom:
                secretKeyRef:
                  name: radarr-4k-secrets
                  key: POSTGRES_LOG_DB
          ports:
            - name: http
              containerPort: 7878
              protocol: TCP
          volumeMounts:
            - name: radarr-4k-config
              mountPath: /config
            - name: media
              mountPath: /media
          readinessProbe:
            httpGet:
              path: /ping
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /ping
              port: http
            initialDelaySeconds: 30
            periodSeconds: 20
      volumes:
        - name: radarr-4k-config
          persistentVolumeClaim:
            claimName: radarr-4k-config-v2
        - name: media
          nfs:
            server: 192.168.1.100
            path: /mnt/epool/media
