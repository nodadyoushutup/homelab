apiVersion: v1
kind: Service
metadata:
  name: cross-seed
spec:
  selector:
    app.kubernetes.io/name: cross-seed
  ports:
    - name: http
      port: 2468
      protocol: TCP
      targetPort: http
