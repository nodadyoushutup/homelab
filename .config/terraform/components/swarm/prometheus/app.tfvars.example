# homelab-config: terraform/components/swarm/prometheus/app
# Self-contained slice tfvars (provider/DNS/NFS inlined; no shared swarm/dns/nfs var-files).

swarm_docker_provider_config = {
  docker = {
    host = ""
    ssh_opts = [
      "", "",
      "", "",
      "", "",
    ]
  }

  registry_auths = [
    {
      address  = ""
      username = ""
      password = ""
    },
    {
      address  = ""
      username = ""
      password = ""
    },
  ]
}

dns_nameservers = [
  "",
  "",
  "",
]

config_path = ""

placement = {
  constraints = [""]
  platforms = [
    {
      os           = ""
      architecture = ""
    },
  ]
}
