# Homelab local environment templates live in `.secrets/.env.example`.
# Copy that file to `.secrets/.env` at the repo root (same directory as the example).
#
# `scripts/terraform/load_root_env.sh` loads `.secrets/.env` (with a short-term
# fallback to repo-root `.env` if `.secrets/.env` is missing). Prefer `.secrets/.env` only.
